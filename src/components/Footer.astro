---
const year = new Date().getFullYear();

const nav = [
  { href: "/projects",     label: "Projects" },
  { href: "/updates",      label: "Updates" },
  { href: "/configurator", label: "Configurator" },
  { href: "/process",      label: "Process" },
  { href: "/about",        label: "About" },
  { href: "/contact",      label: "Contact" },
];

const legal = [
  { href: "/terms",    label: "Terms" },
  { href: "/privacy",  label: "Privacy" },
  { href: "/warranty", label: "Warranty" },
];
---

<footer class="site-footer">

  <div class="container footer-inner">

    <!-- Trust USP strip -->
    <div class="footer-trust-strip" aria-label="Why Draconis Systems">
      <div class="trust-chip">
        <svg width="12" height="12" viewBox="0 0 12 12" fill="none" aria-hidden="true">
          <path d="M6 1l1.3 2.6L10 4.2l-2 2 .5 2.8L6 7.6 3.5 9l.5-2.8-2-2 2.7-.6L6 1z"
            fill="currentColor" opacity="0.8"/>
        </svg>
        <span>1-Year Warranty</span>
      </div>
      <div class="trust-chip">
        <svg width="12" height="12" viewBox="0 0 12 12" fill="none" aria-hidden="true">
          <circle cx="6" cy="6" r="5" stroke="currentColor" stroke-width="1.4" fill="none"/>
          <path d="M4 6l1.5 1.5L8 4" stroke="currentColor" stroke-width="1.4"
            stroke-linecap="round" stroke-linejoin="round"/>
        </svg>
        <span>Stress-Tested Every Build</span>
      </div>
      <div class="trust-chip">
        <svg width="12" height="12" viewBox="0 0 12 12" fill="none" aria-hidden="true">
          <rect x="1" y="3" width="10" height="7" rx="1.5" stroke="currentColor" stroke-width="1.3" fill="none"/>
          <path d="M4 3V2.5a2 2 0 014 0V3" stroke="currentColor" stroke-width="1.3"
            stroke-linecap="round"/>
        </svg>
        <span>UK-Based Studio</span>
      </div>
      <div class="trust-chip">
        <svg width="12" height="12" viewBox="0 0 12 12" fill="none" aria-hidden="true">
          <rect x="1.5" y="1.5" width="9" height="7" rx="1" stroke="currentColor" stroke-width="1.3" fill="none"/>
          <path d="M3.5 10.5h5M6 8.5v2" stroke="currentColor" stroke-width="1.3" stroke-linecap="round"/>
        </svg>
        <span>Complete Handoff Documentation</span>
      </div>
    </div>

    <!-- Brand column -->
    <div class="footer-brand">
      <a href="/" class="footer-brand__mark" aria-label="Draconis Systems home">
        <img src="/images/logo-placeholder.svg" alt="" width="28" height="28" loading="lazy" />
        <span>Draconis<em>Systems</em></span>
      </a>
      <p class="footer-brand__tagline">
        Precision PC engineering, bespoke for your workload.
      </p>
      <a href="mailto:concierge@draconis-systems.co.uk" class="footer-email">
        concierge@draconis-systems.co.uk
      </a>
    </div>

    <!-- Navigate -->
    <nav aria-label="Footer navigation" class="footer-nav">
      <p class="footer-col-label">Navigate</p>
      <ul class="clean footer-links">
        {nav.map(item => (
          <li><a href={item.href} class="footer-link">{item.label}</a></li>
        ))}
      </ul>
    </nav>

    <!-- Legal -->
    <nav aria-label="Legal" class="footer-legal-col">
      <p class="footer-col-label">Legal</p>
      <ul class="clean footer-links">
        {legal.map(item => (
          <li><a href={item.href} class="footer-link">{item.label}</a></li>
        ))}
      </ul>
    </nav>

  </div>

  <!-- Bottom bar -->
  <div class="footer-bottom">
    <div class="container footer-bottom__inner">
      <p class="footer-copy">
        &copy; {year} Draconis Systems Ltd. All rights reserved. UK-based studio.
      </p>
      <div class="footer-bottom__links">
        {legal.map(item => (
          <a href={item.href} class="footer-bottom-link">{item.label}</a>
        ))}
      </div>
    </div>
  </div>

</footer>

<style>
  /* ── Shell ── */
  .site-footer {
    border-top: 1px solid var(--line);
    background: var(--bg-deep, var(--bg));
  }

  :root[data-theme="dark"] .site-footer {
    background: #040810;
  }

  /* ── Main grid ── */
  .footer-inner {
    display: grid;
    grid-template-columns: 2fr 1fr 1fr;
    gap: clamp(2rem, 5vw, 4rem);
    padding-block: clamp(2.5rem, 5vw, 4rem);
  }

  /* ── Trust strip ── */
  .footer-trust-strip {
    grid-column: 1 / -1;
    display: flex;
    flex-wrap: wrap;
    gap: 0.6rem;
    padding-bottom: 1.5rem;
    border-bottom: 1px solid var(--line);
  }

  .trust-chip {
    display: inline-flex;
    align-items: center;
    gap: 0.4rem;
    font-size: 0.7rem;
    font-weight: 600;
    letter-spacing: 0.025em;
    color: var(--text-muted);
    border: 1px solid var(--line);
    border-radius: 999px;
    padding: 0.3rem 0.75rem;
    line-height: 1;
  }

  .trust-chip svg {
    flex-shrink: 0;
    color: var(--brand);
    opacity: 0.75;
  }

  /* ── Brand ── */
  .footer-brand__mark {
    display: inline-flex;
    align-items: center;
    gap: 0.5rem;
    text-decoration: none;
    margin-bottom: 0.9rem;
  }

  .footer-brand__mark img {
    width: 26px;
    height: 26px;
    object-fit: contain;
    opacity: 0.9;
  }

  .footer-brand__mark span {
    font-family: var(--font-display);
    font-size: 0.95rem;
    font-weight: 700;
    color: var(--text-soft);
    letter-spacing: 0.01em;
  }

  .footer-brand__mark em {
    font-style: italic;
    color: var(--brand);
    margin-left: 0.25em;
  }

  .footer-brand__tagline {
    font-size: 0.82rem;
    color: var(--text-muted);
    line-height: 1.6;
    margin-bottom: 0.75rem;
    max-width: 32ch;
  }

  .footer-email {
    font-size: 0.8rem;
    color: var(--text-muted);
    text-decoration: none;
    transition: color 180ms ease;
  }

  .footer-email:hover { color: var(--brand); }

  /* ── Nav columns ── */
  .footer-col-label {
    font-size: 0.68rem;
    font-weight: 700;
    letter-spacing: 0.15em;
    text-transform: uppercase;
    color: var(--text-muted);
    margin-bottom: 1rem;
  }

  .footer-links {
    display: flex;
    flex-direction: column;
    gap: 0.5rem;
  }

  .footer-link {
    font-size: 0.82rem;
    color: var(--text-muted);
    text-decoration: none;
    transition: color 180ms ease;
    display: inline-block;
  }

  .footer-link:hover { color: var(--text); }

  /* ── Bottom bar ── */
  .footer-bottom {
    border-top: 1px solid var(--line);
    padding-block: 1rem;
  }

  .footer-bottom__inner {
    display: flex;
    align-items: center;
    justify-content: space-between;
    flex-wrap: wrap;
    gap: 0.5rem;
  }

  .footer-copy {
    font-size: 0.75rem;
    color: var(--text-muted);
    margin: 0;
  }

  .footer-bottom__links {
    display: flex;
    gap: 1.25rem;
  }

  .footer-bottom-link {
    font-size: 0.75rem;
    color: var(--text-muted);
    text-decoration: none;
    transition: color 180ms ease;
  }

  .footer-bottom-link:hover { color: var(--text); }

  /* ── Responsive ── */
  @media (max-width: 680px) {
    .footer-inner {
      grid-template-columns: 1fr 1fr;
    }

    .footer-brand {
      grid-column: 1 / -1;
    }
  }

  @media (max-width: 420px) {
    .footer-inner {
      grid-template-columns: 1fr;
    }

    .footer-bottom__links { display: none; }
  }
</style>
